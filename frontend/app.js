import {
  Marker,
  Splatter,
  Canvas,
  Palette,
  Eraser,
} from './toolClasses.js'
import {
  splatter,
  getToolElement,
  getPaletteElement,
  getSizeElement
} from './domNodes.js'
import {
  colorMap,
  sizeMap,
} from './maps.js'

document.addEventListener('DOMContentLoaded', function() {
  let canvas = new Canvas()

  debugger
})
